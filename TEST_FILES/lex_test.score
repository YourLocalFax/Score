
type c_str = ^i8

/# "C" is an argument to 'extern',
extern "C" fn puts |c_str| -> i32
/# but it's optional, and "C" is the default.
extern fn putchar |i32| -> i32

/*
fn putb |b: bool| -> () {
	if b {
		puts c"true"
	} el {
		puts c"false"
	}
}

fn puti_ |i: i32 mut| -> () {
    if i < 0 {
        putchar '-'
        i = -i
    }
    if i > 9 {
        puti i / 10
    }
    putchar c'0' + (i % 10)
}

fn puti |i: i32| -> () {
	puti_ i
	puts c""
}
*/

/# _S4main0_Rv
fn main || -> () {
	let hello = c"Hello, world!"
	puts hello
}
